import React from 'react';

let AppContainer = React.createClass({
	render: function() {
		return (
			<div id="AppContainer">

			</div>
		);
	}
});// /<AppContainer />

React.render( <AppContainer />, document.body );